var $$ = function(){};

//框架中实例化，使用时可以直接调用
$$ = new $$();

//获取对象框架  通过id ，标签名 ，类名获取对象
//基础模块
$.extend($$,{
});
//事件模块
$.extend($$,{
});
//选择框架
$.extend($$,{
});
//字符串相关操作框架
$.extend($$,{
});
//日期相关操作模块
$.extend($$,{
});
